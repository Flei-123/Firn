// expect_exit: 89
// Fibonacci, rekursiv: fib(11) = 89 (passt in einen Exit-Code).

fn fib(n: i32) -> i32 {
    if n < 2 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() -> i32 {
    return fib(11)
}
