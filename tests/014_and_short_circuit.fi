// expect_exit: 7
// With && the right-hand side must not be evaluated (division by 0 otherwise).
fn boom(n: i32) -> i32 {
    return 100 / n
}
fn main() -> i32 {
    let z: i32 = 0
    var r: i32 = 7
    if z != 0 && boom(z) > 0 {
        r = 1
    }
    return r
}
