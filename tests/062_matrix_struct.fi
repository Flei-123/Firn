// expect_exit: 32
struct Vec2 {
    x: i32,
    y: i32,
}
fn dot(a: *Vec2, b: *Vec2) -> i32 {
    return (*a).x * (*b).x + (*a).y * (*b).y
}
fn main() -> i32 {
    var u: Vec2 = Vec2 { x: 2, y: 4, }
    var v: Vec2 = Vec2 { x: 6, y: 5, }
    return dot(&u, &v)
}
