// expect_exit: 7
struct P {
    x: i32,
    y: i32,
}
fn main() -> i32 {
    var a: P = P { x: 3, y: 4, }
    var b: P = a // Kopie des ganzen Aggregats (copymem)
    a.x = 100 // darf b nicht veraendern
    return b.x + b.y
}
