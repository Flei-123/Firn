import std.io
import std.rt
fn add(a: f64, b: f64) -> f64 {
    return a * b + 1.5
}
fn main() -> i32 {
    var x: f64 = 2.0
    var y: f64 = 3.0
    var s: f64 = 0.0
    var i: i32 = 0
    while i < 10 {
        s = s + add(x, y)
        i = i + 1
    }
    return (s as i32)
}
