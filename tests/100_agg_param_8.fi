// expect_exit: 30
// Struct of 8 bytes as a parameter: System V INTEGER class, one word (abi.rs).
struct P { x: i32, y: i32 }

fn sum(p: P) -> i32 {
    return p.x + p.y
}

fn main() -> i32 {
    let p: P = P { x: 10, y: 20 }
    return sum(p)
}
