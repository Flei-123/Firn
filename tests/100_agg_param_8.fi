// expect_exit: 30
// Struct mit 8 Byte als Parameter: System-V-INTEGER-Klasse, ein Wort (abi.rs).
struct P { x: i32, y: i32 }

fn sum(p: P) -> i32 {
    return p.x + p.y
}

fn main() -> i32 {
    let p: P = P{ x: 10, y: 20 }
    return sum(p)
}
