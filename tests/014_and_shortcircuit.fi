// expect_exit: 7
// Bei && darf die rechte Seite nicht ausgewertet werden (sonst Division durch 0).
fn boom(n: i32) -> i32 {
    return 100 / n
}
fn main() -> i32 {
    let z: i32 = 0
    var r: i32 = 7
    if z != 0 && boom(z) > 0 {
        r = 1
    }
    return r
}
