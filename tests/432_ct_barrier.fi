// expect_exit: 42
// SPEC §9.2: barrier(x) liefert x unveraendert, gilt aber als undurchschaubar.
fn main() -> i32 {
    let a: i32 = barrier(21 as i32)
    let b: i32 = barrier(a)
    return a + b
}
