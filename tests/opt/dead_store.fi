// expect_exit: 7
// Tote Speicherung: in `t` wird geschrieben, aber nie daraus gelesen.
// alloca + store muessen komplett verschwinden.
fn main() -> i32 {
    var t: i32 = 5
    t = t + 1
    t = t * 3
    return 7
}
