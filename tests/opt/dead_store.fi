// expect_exit: 7
// Dead store: `t` is written to but never read from.
// alloca + store have to disappear completely.
fn main() -> i32 {
    var t: i32 = 5
    t = t + 1
    t = t * 3
    return 7
}
