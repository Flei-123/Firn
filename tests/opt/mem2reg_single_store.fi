// expect_exit: 42
// mem2reg: `x` wird genau einmal geschrieben und in einem ANDEREN Block
// gelesen. Der store dominiert den load -> der load verschwindet.
fn main() -> i32 {
    let x: i32 = 40
    var s: i32 = 0
    if x > 0 {
        s = x + 2
    }
    return s
}
