// expect_exit: 42
// mem2reg: `x` is written exactly once and read in ANOTHER block.
// The store dominates the load -> the load disappears.
fn main() -> i32 {
    let x: i32 = 40
    var s: i32 = 0
    if x > 0 {
        s = x + 2
    }
    return s
}
