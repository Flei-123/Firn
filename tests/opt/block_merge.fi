// expect_exit: 3
// Block merging: the empty then block is a pure `br` and is
// bridged, after that the remaining blocks merge.
fn main() -> i32 {
    var x: i32 = 0
    if x == 0 {
    }
    while x > 10 {
    }
    return 3
}
