// expect_exit: 11
struct P {
    x: i32,
    y: i32,
}
fn shift(p: *mut P, d: i32) {
    (*p).x = (*p).x + d
    (*p).y = (*p).y + d
}
fn main() -> i32 {
    var p: P = P { x: 1, y: 2, }
    shift(&p, 4)
    return p.x + p.y
}
