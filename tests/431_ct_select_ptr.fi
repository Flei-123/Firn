// expect_exit: 7
// select on pointers and u8: the type of the result is the type of the arms.
fn main() -> i32 {
    var a: u8 = 7
    var b: u8 = 9
    let pa: *u8 = &a
    let pb: *u8 = &b
    let p: *u8 = select(a < b, pa, pb)
    let w: u8 = select(true, *p, 0 as u8)
    return w as i32
}
