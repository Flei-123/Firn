// expect_error: 14:14 type 'Dot' has no method 'diff'
// expect_error_count: 1
// Eine Methode, die es nicht gibt, wird als solche gemeldet — mit dem TYP im
// Text und den vorhandenen Methoden im Hinweis. Der Aufruf faellt NICHT auf
// eine gleichnamige freie Funktion zurueck (compiler/src/impls.rs).
struct Dot { x: i32, y: i32 }

impl Dot {
    fn sum(*self) -> i32 { return (*self).x + (*self).y }
}

fn main() -> i32 {
    var p: Dot = Dot{ x: 1, y: 2 }
    return p.diff()
}
