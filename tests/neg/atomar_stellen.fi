// expect_error: 7:18 erwartet genau zwei argumente
// RUNDE 47 — falsche Stellenzahl beim atomaren Primitiv. Die Meldung nennt
// die vereinbarte Form, damit man nicht raten muss.

fn main() -> i32 {
    var z: u64 = 5
    let a: u64 = __atomar_addieren(&z)
    if a == 0 {
        return 1
    }
    return 0
}
