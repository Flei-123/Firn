// expect_error: 8:5 the result must not be discarded: the type 'E!i32' is marked with #[must_consume]
// Ein '!T'-Wert ist implizit #[must_consume] (SPEC §5.1).
error E { A }

fn f() -> E!i32 { return 1 }

fn main() -> i32 {
    f()
    return 0
}
