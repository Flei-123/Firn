// expect_error: 6:1 module 'math' is imported more than once
// expect_error_count: 1
// The same module twice in ONE file. Two DIFFERENT files importing it is
// perfectly ordinary and stays allowed -- that is the whole compiler.
import modules.math
import modules.math

fn main() -> i32 {
    return modules.math.DREI
}
