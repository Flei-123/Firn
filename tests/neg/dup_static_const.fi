// expect_error: 5:1 'shared' is already declared
// expect_error_count: 1
// A `static` and a `const` are both names at the top level.
const shared: i64 = 1
static shared: i64 = 2

fn main() -> i32 {
    return shared as i32
}
