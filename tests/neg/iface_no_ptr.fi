// expect_error: 11:23 ein schnittstellenwert entsteht aus einem zeiger, gefunden i64
// expect_error_count: 1
// Ein Schnittstellenwert entsteht aus einem Zeiger auf einen struct — nicht
// aus einer Zahl.
interface Area {
    fn area(*self) -> i64
}

fn main() -> i32 {
    let x: i64 = 5
    let f: dyn Area = x as dyn Area
    return 0
}
