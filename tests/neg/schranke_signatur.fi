// expect_error: 13:6 parameter 1 von 'Punkt.kleiner' hat typ *i64, die schnittstelle 'Ordnung' verlangt *Punkt
// expect_error_count: 1
// Der `impl`-Block ist da, aber die Signatur passt nicht: `Self` steht in der
// Schnittstelle fuer den umsetzenden Typ, hier also fuer `*Punkt`. Diese
// Pruefung gehoert dem Typpruefer (`iface::pruefe_umsetzung`) — die Schranke
// selbst ist erfuellt, es GIBT ja einen Block.
interface Ordnung {
    fn kleiner(*self, b: *Self) -> bool
}

struct Punkt { x: i64 }

impl Ordnung for Punkt {
    fn kleiner(*self, b: *i64) -> bool { return (*self).x < *b }
}

fn main() -> i32 {
    return 0
}
