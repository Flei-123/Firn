// expect_error: 6:45 unknown name 'later'
fn apply(f: fn() -> i32) -> i32 { return f() }

fn main() -> i32 {
    // `later` is declared only AFTER the closure -- outside its lifetime.
    let r: i32 = apply(fn() -> i32 { return later })
    let later: i32 = 5
    return r
}
