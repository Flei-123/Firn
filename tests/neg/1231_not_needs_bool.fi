// expect_error: 6:18 unary '!' expects the type bool, found i32
// ROUND 68: `!` stays the LOGICAL negation. Since `~` exists the note
// beside this message points at it instead of at 'x ^ -1'.
fn main() -> i32 {
    let a: i32 = 1
    let b: i32 = !a
    return b
}
