// expect_error: 13:6 'Kreis' setzt die methode 'Flaeche.skaliere' nicht um
// expect_error_count: 1
// Eine Umsetzung, in der eine Methode der Schnittstelle fehlt. Gemeldet wird
// an der Schnittstelle im `impl`-Kopf, mit der erwarteten Signatur im
// Hinweis (compiler/src/iface.rs).
interface Flaeche {
    fn flaeche(*self) -> i64
    fn skaliere(*mut self, k: i64)
}

struct Kreis { r: i64 }

impl Flaeche for Kreis {
    fn flaeche(*self) -> i64 { return (*self).r }
}

fn main() -> i32 {
    return 0
}
