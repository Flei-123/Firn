// expect_error: 6:18 'GEHEIM' is not exported by module 'math'
import modules.math

fn main() -> i32 {
    // yet. The attempt is reported, not silently compiled wrongly.
    let x: i32 = math.GEHEIM
    return x
}
