// expect_error: 15:6 'Circle' already implements the interface 'Area'
// expect_error_count: 1
// Zwei `impl`-Bloecke fuer DIESELBE Schnittstelle und denselben Typ. Welche
// Methodentafel gaelte? Die Frage darf gar nicht erst entstehen.
interface Area {
    fn area(*self) -> i64
}

struct Circle { r: i64 }

impl Area for Circle {
    fn area(*self) -> i64 { return (*self).r }
}

impl Area for Circle {
    fn extent(*self) -> i64 { return (*self).r }
}

fn main() -> i32 {
    return 0
}
