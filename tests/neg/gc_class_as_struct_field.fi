// expect_error: 5:20 'Node' is a gc class and cannot be a value
// SPEC 4.4: `.as?[T]` only checks inside an inheritance chain.
gc class Node { id: u32 }

struct Holder { n: Node }

fn main() -> i32 {
    return 0
}
