// expect_error: 5:20 'Node' ist eine gc-klasse und kann kein wert sein
// SPEC §3.5.1: kein Feld eines gewoehnlichen `struct`.
gc class Node { id: u32 }

struct Holder { n: Node }

fn main() -> i32 {
    return 0
}
