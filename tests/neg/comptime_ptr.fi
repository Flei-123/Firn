// expect_error: 6:23 comptime
// Zeiger brauchen einen Speicher zur Uebersetzungszeit — den gibt es noch
// nicht. Der Versuch wird gemeldet, nicht still falsch uebersetzt.
fn address() -> i64 {
    var x: i64 = 5
    let p: *mut i64 = &x
    return *p
}

const Y: i64 = address()

fn main() -> i32 {
    return Y as i32
}
