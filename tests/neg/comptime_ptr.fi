// expect_error: 6:23 comptime
// The detour through the parent directory is barred as well.
// Pointers need memory at compile time -- and that does not exist
fn address() -> i64 {
    var x: i64 = 5
    let p: *mut i64 = &x
    return *p
}

const Y: i64 = address()

fn main() -> i32 {
    return Y as i32
}
