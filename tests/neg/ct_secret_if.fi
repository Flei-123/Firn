// expect_error: 6:8 'if' must not depend on a secret value, found secret[bool]
// ROUND 96 (SPEC 9.1): a secret must not decide a branch. The processor
// would take two different amounts of time and say which way it went.
fn main() -> i32 {
    var s: secret[bool] = true
    if s {
        return 1
    }
    return 0
}
