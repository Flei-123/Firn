// expect_error: 8:14 type 'i32' has no method 'sum'
// expect_error_count: 1
// Since round 50 a BASIC TYPE may have methods as well (`impl Ord for i32`).
// That is why the message no longer says "only for struct types" but names
// the type and the method looked for -- and in the note what exists for 'i32'.
fn main() -> i32 {
    var n: i32 = 3
    return n.sum()
}
