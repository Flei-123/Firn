// expect_error: 9:19 die schranke 'Ordnung' steht zweimal an 'T'
// expect_error_count: 1
// Dieselbe Schranke zweimal. Das ist keine Verschaerfung, sondern ein
// Schreibfehler — gemeldet beim Parsen, nicht erst bei der Auspraegung.
interface Ordnung {
    fn kleiner(*self, b: *Self) -> bool
}

fn f[T: Ordnung + Ordnung](a: *T) -> bool {
    return a.kleiner(a)
}

fn main() -> i32 {
    return 0
}
