// expect_error: 5:29 conversion from i32 to fn(i32) -> i32 is not allowed
// Round GAPS: only u64/usize become a function value (fnaddr.rs).
fn main() -> i32 {
    let x: i32 = 3
    let f: fn(i32) -> i32 = x as fn(i32) -> i32
    return 0 as i32
}
