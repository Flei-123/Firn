// expect_error: 10:19 a text constant is built from text literals
// ROUND FIRN-ENV -- what a text constant may be built from: text literals,
// text constants declared before it, and `+`. NOT a call: everything a
// function could compute belongs in `comptime`, which owns the interpreter
// with the step limit.
fn name() -> str {
    return "abc"
}

const NAME: str = name()

fn main() -> i32 {
    return 0
}
