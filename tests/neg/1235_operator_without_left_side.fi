// expect_error: 6:5 expected an expression, found '&&'
// ROUND 68: the continuation joins to the expression of the line BEFORE.
// Where there is none -- directly after '{' -- a leading operator stays what
// it always was: a syntax error, with line and column.
fn main() -> i32 {
    && true
    return 0
}
