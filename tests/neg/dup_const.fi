// expect_error: 8:1 constant 'LIMIT' is already declared
// expect_error_count: 1
// ROUND 96: the case that started the round. The merge of K4 and K5 left
// `const KDATA_SIZE` twice in demos/kernel/kstate.fi -- firnc0 refused,
// firnc1 built it with the FIRST of the two values and let the kernel write
// its tables outside the data area. A name goes into a table ONLY if it is
// not in it yet.
const LIMIT: u64 = 0x30000
const LIMIT: u64 = 0x20000

fn main() -> i32 {
    return LIMIT as i32
}
