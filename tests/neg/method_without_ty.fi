// expect_error: 8:14 method 'sum' on a value of type [i32; 3] — this type cannot have methods
// expect_error_count: 1
// A field is not a named type: there is no name under which an
// `impl` block could stand. Struct types and basic types can have
// methods (round 45 resp. round 50), a field type cannot.
fn main() -> i32 {
    var a: [i32; 3] = [1, 2, 3]
    return a.sum()
}
