// expect_error: 8:14 type 'Ctx' has no method 'hook'
// ROUND 68, the counter-check: only a field of FUNCTION type is callable.
// A field of another type leaves the message exactly as it was before this
// round -- the new path does not swallow the old error.
struct Ctx { hook: i32 }
fn main() -> i32 {
    var c: Ctx = Ctx { hook: 1 }
    return c.hook(1, 2)
}
