// expect_error: 6:11 'while' must not depend on a secret value, found secret[bool]
// ROUND 96 (SPEC 9.1): a `while` is a branch too, and it is a branch that
// says how OFTEN it went that way.
fn main() -> i32 {
    var s: secret[bool] = true
    while s {
        return 1
    }
    return 0
}
