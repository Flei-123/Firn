// expect_error: 5:15 comptime: '../geheim.txt' enthaelt '..'
// Auch der Umweg ueber das Elternverzeichnis ist versperrt.
comptime {
    emit_roh("const X: i64 = ")
    emit_zahl(datei_groesse("../geheim.txt"))
    emit_roh("\n")
}

fn main() -> i32 {
    return X as i32
}
