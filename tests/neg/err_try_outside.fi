// expect_error: 8:13 'try' ist nur in einer funktion mit fehlerunions-rueckgabetyp erlaubt
// 'try' in einer Funktion, die keine Fehlerunion liefert.
error E { A }

fn f() -> E!i32 { return E::A }

fn main() -> i32 {
    let x = try f()
    return x
}
