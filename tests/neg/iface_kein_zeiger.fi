// expect_error: 11:26 ein schnittstellenwert entsteht aus einem zeiger, gefunden i64
// expect_error_count: 1
// Ein Schnittstellenwert entsteht aus einem Zeiger auf einen struct — nicht
// aus einer Zahl.
interface Flaeche {
    fn flaeche(*self) -> i64
}

fn main() -> i32 {
    let x: i64 = 5
    let f: dyn Flaeche = x as dyn Flaeche
    return 0
}
