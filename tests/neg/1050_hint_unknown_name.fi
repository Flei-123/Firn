// expect_error: 6:18 did you mean 'value'?
// ROUND 64: a typo in a name. The compiler knows the right names -- it only
// has to say them (compiler/src/diag.rs::nearest).
fn main() -> i32 {
    let value: i32 = 3
    let a: i32 = valu
    return a
}
