// Module 'nogc_cold' -- the counter-check to tests/modules/nogc_hot.fi.
// 'costly' carries NO #[no_gc]; whoever calls it from a #[no_gc] call tree
// gets an error (tests/neg/nogc_module_boundary.fi).
export { costly }

fn costly(a: i32) -> i32 {
    return a * 3 as i32
}
