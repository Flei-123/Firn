// Modul 'nogc_kalt' — Gegenprobe zu tests/modules/nogc_hot.fi.
// 'aufwaendig' traegt KEIN #[no_gc]; wer es aus einem #[no_gc]-Aufrufbaum
// ruft, bekommt einen Fehler (tests/neg/nogc_modulgrenze.fi).
export { costly }

fn costly(a: i32) -> i32 {
    return a * 3 as i32
}
