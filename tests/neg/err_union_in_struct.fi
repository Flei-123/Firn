// expect_error: 6:15 eine fehlerunion ueber dem erfolgstyp 'P' kann nicht feldtyp eines structs sein
// Struct-Layouts stehen bei der Feldaufloesung noch nicht fest (SPEC 14.1.fehlerunionen F10).
error E { A }

struct P { x: i32 }
struct H { r: E!P }

fn main() -> i32 {
    return 0
}
