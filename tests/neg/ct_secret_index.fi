// expect_error: 7:18 an index must not be a secret value, found secret[usize]
// ROUND 96 (SPEC 9.1, C4): a secret index puts the secret into the address
// and the address into the cache.
fn main() -> i32 {
    var table: [i32; 4] = [1, 2, 3, 4]
    var i: secret[usize] = 2
    return table[i]
}
