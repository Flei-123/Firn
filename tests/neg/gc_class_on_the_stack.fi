// expect_error: 6:12 'Knoten' ist eine gc-klasse und kann kein wert sein
// SPEC §3.5.1: ein `gc class`-Wert lebt NUR auf dem GC-Heap.
gc class Node { id: u32 }

fn main() -> i32 {
    let x: Node = 0 as i32
    return 0
}
