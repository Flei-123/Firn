// expect_error: 4:16 a floating point constant is built from literals
// ROUND FIRN-LUECKEN: and a cast is the same door with a friendlier name.
const N: usize = 100
const A: f64 = N as f64
fn main() -> i32 {
    return 0
}
