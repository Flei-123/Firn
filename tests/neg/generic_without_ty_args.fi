// expect_error: 7:13 generic function 'duplicate' needs type arguments, e.g. 'duplicate[i32](..)'
fn duplicate[T: Int](a: T) -> T {
    return a + a
}

fn main() -> i32 {
    let x = duplicate(2 as i32)
    return 0 as i32
}
