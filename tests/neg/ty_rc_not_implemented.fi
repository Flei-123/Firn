// expect_error: 8:12 'Rc[T]' is not implemented in stage 0
// ROUND 96 -- successor of tests/neg/int_secret_not_implemented.fi, which
// asked for `secret[T]` and was answered by this round.
//
// The guard it stood for stays: what the SPEC describes and stage 0 does not
// build has to report a CLEAR error with a line and a column -- no syntax
// error, no crash, no silence. `Rc[T]` (SPEC 3.4) is such a construct.
fn hold(x: Rc[i32]) -> i32 {
    return 0
}

fn main() -> i32 {
    return 0
}
