// expect_error: 7:16 'x' is already declared in this block
// expect_error_count: 1
enum Pair { Two(i32, i32) }

fn main() -> i32 {
    let p = Pair::Two(1 as i32, 2 as i32)
    match p {
        Pair::Two(x, x) => { return x }
    }
}
