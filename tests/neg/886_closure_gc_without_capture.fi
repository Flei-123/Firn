// expect_error: 4:29 this closure captures nothing and needs no GC record
gc class Dummy { v: i64 }
fn main() -> i32 {
    let f: fn(i32) -> i32 = gc fn(a: i32) -> i32 { return a + 1 }
    return f(1)
}
