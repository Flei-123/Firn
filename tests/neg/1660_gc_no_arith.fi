// expect_error: 4:23 operator '+' expects two operands of the same integer type, found Gc[Node] and usize
gc class Node { v: i64 }
fn f(n: Gc[Node]) -> i32 {
    let m: Gc[Node] = n + 1 as usize
    return 0 as i32
}
fn main() -> i32 { return 0 as i32 }
