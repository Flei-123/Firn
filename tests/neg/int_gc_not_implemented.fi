// expect_error: 6:12 unknown gc class 'i32'
// Integration: the GC from SPEC 3.5 is built by now (round "hard test 2").
// `Gc[T]` therefore no longer carries the message "not implemented" but
// checks its type argument: `Gc[T]` only exists over a `gc class`.
fn main() -> i32 {
    let x: Gc[i32] = 0 as i32
    return 0
}
