// expect_error: 8:14 methoden gibt es nur fuer struct-typen
// expect_error_count: 1
// Methoden gibt es fuer struct-Typen — nicht fuer Ganzzahlen. Der Fehler
// nennt den gefundenen Typ, damit die Ursache (falsche Variable) sichtbar
// wird.
fn main() -> i32 {
    var n: i32 = 3
    return n.summe()
}
