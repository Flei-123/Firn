// expect_error: 6:17 that is the blank U+00A0, not the blank U+0020
// ROUND 64: a non-breaking blank. Until now `firnc0` swallowed it silently
// (char::is_whitespace) while `firnc1` reported it -- the two lexers
// disagreed. Now both say the same thing.
fn main() -> i32 {
    let e: i32 =  1
    return e
}
