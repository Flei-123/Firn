// expect_error: 5:5 comptime: more than 2000000 steps — endless loop?
// instantiation -- but with the list of known names.
fn endless() -> i64 {
    var i: i64 = 0
    while i >= 0 {
        i = i + 1
    }
    return i
}

const X: i64 = endless()

fn main() -> i32 {
    return X as i32
}
