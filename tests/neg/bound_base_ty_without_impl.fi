// expect_error: 21:8 typ 'u8' setzt die schnittstelle 'Order' nicht um
// expect_error_count: 1
// Ein GRUNDTYP ohne Umsetzung. Seit Runde 50 darf `i32` eine Schnittstelle
// umsetzen — automatisch tut er es nicht. Die Meldung nennt die Methode, die
// in `impl Ordnung for u8` stehen muesste.
interface Order {
    fn less(*self, b: *Self) -> bool
}

impl Order for i32 {
    fn less(*self, b: *i32) -> bool { return *self < *b }
}

fn f[T: Order](a: *T, b: *T) -> bool {
    return a.less(b)
}

fn main() -> i32 {
    var x: u8 = 1
    var y: u8 = 2
    if f[u8](&x, &y) {
        return 1
    }
    return 0
}
