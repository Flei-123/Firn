// expect_error: 9:12 the first parameter of a method is the receiver: 'self', '*self' or '*mut self'
// expect_error_count: 1
// Associated functions without a receiver (`Type::new(..)`) do not exist in
// this round (docs/ROUND45.md, "deliberately left out"). Whoever writes them
// gets a clear statement instead of a helpless syntax error.
struct Dot { x: i32, y: i32 }

impl Dot {
    fn new(x: i32, y: i32) -> Dot { return Dot { x: x, y: y } }
}

fn main() -> i32 {
    return 0
}
