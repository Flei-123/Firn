// expect_error: 10:6 parameter 1 von 'Circle.scale' hat typ bool, die schnittstelle 'Area' verlangt i64
// expect_error_count: 1
// Der Parametertyp passt nicht zur Schnittstelle.
interface Area {
    fn scale(*mut self, k: i64)
}

struct Circle { r: i64 }

impl Area for Circle {
    fn scale(*mut self, k: bool) { (*self).r = 1 }
}

fn main() -> i32 {
    return 0
}
