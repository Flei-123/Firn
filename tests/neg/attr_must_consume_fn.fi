// expect_error: 6:5 the result must not be discarded: 'reserve' is marked with #[must_consume]
#[must_consume]
fn reserve(n: i32) -> i32 { return n * 2 }

fn main() -> i32 {
    reserve(3)
    return 0
}
