// expect_error: 5:16 parameter 'a' is already declared
// expect_error_count: 2
// TWO messages, and both belong: the signature is wrong (collect_fns) and
// the body would have two bindings under one name (declare_var).
fn add(a: i32, a: i32) -> i32 {
    return a
}

fn main() -> i32 {
    return add(1 as i32, 2 as i32)
}
