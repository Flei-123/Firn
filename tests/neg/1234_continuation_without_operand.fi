// expect_error: 8:5 expected an expression, found 'return'
// ROUND 68: a line continuation still has to end in an EXPRESSION. The
// operator at the end of the line opens the continuation; what follows it
// has to be an operand and not the next statement.
fn main() -> i32 {
    let x: i32 = 1
    +
    return x
}
