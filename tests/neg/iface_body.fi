// expect_error: 6:27 an interface method has no body ('Area.area')
// expect_error_count: 1
// #[must_consume] and must not lapse silently.
// An interface declares signatures, not bodies. Default methods
interface Area {
    fn area(*self) -> i64 { return 1 }
}

fn main() -> i32 {
    return 0
}
