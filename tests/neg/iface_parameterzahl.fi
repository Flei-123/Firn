// expect_error: 11:6 'Circle.scale' has 2 parameters, the interface 'Area' requires 1
// expect_error_count: 1
// Zu viele Parameter. Gezaehlt wird OHNE den Empfaenger — genau wie am
// Aufrufort (impls.rs, Runde 45).
interface Area {
    fn scale(*mut self, k: i64)
}

struct Circle { r: i64 }

impl Area for Circle {
    fn scale(*mut self, k: i64, m: i64) { (*self).r = k + m }
}

fn main() -> i32 {
    return 0
}
