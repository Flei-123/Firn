// expect_error: 11:6 'Kreis.skaliere' hat 2 parameter, die schnittstelle 'Flaeche' verlangt 1
// expect_error_count: 1
// Zu viele Parameter. Gezaehlt wird OHNE den Empfaenger — genau wie am
// Aufrufort (impls.rs, Runde 45).
interface Flaeche {
    fn skaliere(*mut self, k: i64)
}

struct Kreis { r: i64 }

impl Flaeche for Kreis {
    fn skaliere(*mut self, k: i64, m: i64) { (*self).r = k + m }
}

fn main() -> i32 {
    return 0
}
