// expect_error: 6:18 a u"…" literal has no str type
// ROUND 70: `str` holds OCTETS. A `u"..."` literal is a sequence of u16 code
// units (WTF-16, SPEC 8.2) and has no str type -- whoever wants it writes
// the array type.
fn main() -> i32 {
    let s: str = u"abc"
    return s.n as i32
}
