// expect_error: 5:5 Firn has no annotations with '@' -- an attribute is written '#[name]'
// ROUND 64: an annotation with '@' out of another language.
fn main() -> i32 {
    let a: i32 = 1
    @inline
    return a
}
