// expect_error: 7:13 the receiver of an interface method is '*self' or '*mut self' ('Area.area')
// expect_error_count: 1
// The receiver of an interface method MUST be a pointer: through the
// table only the data pointer is available, and the caller could not
// build a copy of the value at all -- it does not know the type.
interface Area {
    fn area(self) -> i64
}

fn main() -> i32 {
    return 0
}
