// expect_error: 13:5 field type dyn Area is not allowed in a gc class
// expect_error_count: 1
// Ein Schnittstellenwert darf nicht im Heap liegen: der Sammler verfolgt dort
// PRAEZISE anhand des Feldlayouts (SPEC 3.5.3) und kennt den Datenzeiger
// hinter `dyn` nicht. Ein Loch in dieser Zusage waere schlimmer als eine
// fehlende Bequemlichkeit (docs/RUNDE46.md).
interface Area {
    fn area(*self) -> i64
}

gc class Node {
    value: i64,
    f: dyn Area,
}

fn main() -> i32 {
    return 0
}
