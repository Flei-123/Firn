// expect_error: 6:34 the output operand for 'rax' has type u64, expected a pointer
// ROUND 68: an output operand names the ADDRESS it writes to. A value
// cannot be written to -- the address operator is missing here.
fn main() -> i32 {
    var a: u64 = 0
    asm("mov rax, 1", out("rax") a)
    return 0
}
