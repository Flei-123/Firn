// expect_error: 10:12 'hot' is #[no_gc], but calls 'nogc_cold.costly' without #[no_gc]
// expect_error_count: 1
// Rule (ii) across the MODULE BOUNDARY (SPEC 3.5.4). The module system
// rewrites the name internally to 'nogc_cold__costly'; the message shows
// the spelling from the source text again.
import modules.nogc_cold

#[no_gc]
fn hot(a: i32) -> i32 {
    return nogc_cold.costly(a)
}

fn main() -> i32 {
    return hot(0 as i32)
}
