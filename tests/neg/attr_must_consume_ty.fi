// expect_error: 8:5 the result must not be discarded: the type 'Guard' is marked with #[must_consume]
#[must_consume]
struct Guard { fd: i32 }

fn open(fd: i32) -> Guard { return Guard { fd: fd, } }

fn main() -> i32 {
    open(7)
    return 0
}
