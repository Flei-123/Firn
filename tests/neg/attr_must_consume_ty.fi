// expect_error: 8:5 das ergebnis darf nicht verworfen werden
#[must_consume]
struct Guard { fd: i32 }

fn open(fd: i32) -> Guard { return Guard{ fd: fd, } }

fn main() -> i32 {
    open(7)
    return 0
}
