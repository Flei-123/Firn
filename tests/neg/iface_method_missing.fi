// expect_error: 13:6 'Circle' does not implement the method 'Area.scale'
// expect_error_count: 1
// An implementation in which a method of the interface is missing. It is
// reported at the interface in the `impl` head, with the expected signature
// in the note (compiler/src/iface.rs).
interface Area {
    fn area(*self) -> i64
    fn scale(*mut self, k: i64)
}

struct Circle { r: i64 }

impl Area for Circle {
    fn area(*self) -> i64 { return (*self).r }
}

fn main() -> i32 {
    return 0
}
