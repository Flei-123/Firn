// expect_error: 13:6 'Circle' setzt die methode 'Area.scale' nicht um
// expect_error_count: 1
// Eine Umsetzung, in der eine Methode der Schnittstelle fehlt. Gemeldet wird
// an der Schnittstelle im `impl`-Kopf, mit der erwarteten Signatur im
// Hinweis (compiler/src/iface.rs).
interface Area {
    fn area(*self) -> i64
    fn scale(*mut self, k: i64)
}

struct Circle { r: i64 }

impl Area for Circle {
    fn area(*self) -> i64 { return (*self).r }
}

fn main() -> i32 {
    return 0
}
