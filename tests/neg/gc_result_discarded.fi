// expect_error: 7:5 the result must not be discarded: the type 'AllocError!Gc[Node]' is marked with #[must_consume]
// DESIGNZIELE §2: GC-Allokation ist fehlbar; `AllocError!Gc[T]` ist
// #[must_consume] und darf nicht stillschweigend verfallen.
gc class Node { id: u32 }

fn main() -> i32 {
    gc Node{ id: 1 }
    return 0
}
