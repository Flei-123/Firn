// expect_error: 7:34 did you mean 'height'?
// ROUND 64: a typo in a field name INSIDE a struct literal.
struct Box { width: i32, height: i32 }

fn main() -> i32 {
    let b: Box = Box { width: 1, height: 2 }
    let c: Box = Box { width: 1, heigth: 2 }
    return b.width + c.width
}
