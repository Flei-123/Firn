// expect_error: 18:24 'Dot' does not implement the interface 'Area'
// expect_error_count: 1
// do not exist at this stage (docs/ROUND46.md).
// A type that does not implement the interface does not become an
interface Area {
    fn area(*self) -> i64
}

struct Circle { r: i64 }
struct Dot { x: i64 }

impl Area for Circle {
    fn area(*self) -> i64 { return (*self).r }
}

fn main() -> i32 {
    var p: Dot = Dot { x: 1 }
    let f: dyn Area = (&p) as dyn Area
    return 0
}
