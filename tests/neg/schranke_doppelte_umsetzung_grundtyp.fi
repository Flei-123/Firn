// expect_error: 15:6 'i32' setzt die schnittstelle 'Ordnung' bereits um
// expect_error_count: 2
// Zweimal dieselbe Umsetzung fuer denselben Grundtyp. Verglichen wird der
// METHODENPRAEFIX des Traegers, nicht der Structindex — sonst faende die
// Pruefung fuer Grundtypen (Index `usize::MAX`) jede Umsetzung doppelt oder
// keine. Der zweite Block erzeugt zusaetzlich eine doppelte Funktion.
interface Ordnung {
    fn kleiner(*self, b: *Self) -> bool
}

impl Ordnung for i32 {
    fn kleiner(*self, b: *i32) -> bool { return *self < *b }
}

impl Ordnung for i32 {
    fn kleiner(*self, b: *i32) -> bool { return *b < *self }
}

fn main() -> i32 {
    return 0
}
