// expect_error: 5:17 comptime: '../geheim.txt' enthaelt '..'
// Auch der Umweg ueber das Elternverzeichnis ist versperrt.
comptime {
    emit_raw("const X: i64 = ")
    emit_number(file_size("../geheim.txt"))
    emit_raw("\n")
}

fn main() -> i32 {
    return X as i32
}
