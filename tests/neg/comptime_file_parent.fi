// expect_error: 5:17 comptime: '../geheim.txt' contains '..'
// attacks. Therefore: only relative to the source file (DESIGN_GOALS 3).
comptime {
    emit_raw("const X: i64 = ")
    emit_number(file_size("../geheim.txt"))
    emit_raw("\n")
}

fn main() -> i32 {
    return X as i32
}
