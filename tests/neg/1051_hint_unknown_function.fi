// expect_error: 7:18 did you mean 'double'?
// ROUND 64: a typo in a function name. Functions are values since round 58,
// so the candidate set is the same one as for a name.
fn double(v: i32) -> i32 { return v * 2 }

fn main() -> i32 {
    let a: i32 = doubl(3)
    return a
}
