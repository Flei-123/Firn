// expect_error: 5:17 ist in einer gc-klasse nicht erlaubt
// Erlaubt sind Skalare, Gc[T], GcWeak[T] und Arrays davon.
struct Dot { x: i32 }

gc class Node { p: Dot }

fn main() -> i32 {
    return 0
}
