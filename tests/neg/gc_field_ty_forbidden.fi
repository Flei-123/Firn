// expect_error: 5:17 field type Dot is not allowed in a gc class
// SPEC 3.5.1: a `gc class` value lives ONLY on the GC heap.
struct Dot { x: i32 }

gc class Node { p: Dot }

fn main() -> i32 {
    return 0
}
