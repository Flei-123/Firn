// expect_error: 5:19 ist in einer gc-klasse nicht erlaubt
// Erlaubt sind Skalare, Gc[T], GcWeak[T] und Arrays davon.
struct Punkt { x: i32 }

gc class Knoten { p: Punkt }

fn main() -> i32 {
    return 0
}
