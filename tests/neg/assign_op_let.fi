// expect_error: 7:5 'x' is bound with 'let' and cannot be modified
// ROUND 70: `let` stays immutable. `x += 1` is exactly `x = x + 1`, so it
// runs into exactly the same wall -- with the same message, out of the same
// lvalue check.
fn main() -> i32 {
    let x = 5
    x += 1
    return x
}
