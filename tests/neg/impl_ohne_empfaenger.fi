// expect_error: 9:12 der erste parameter einer methode ist der empfaenger
// expect_error_count: 1
// Zugeordnete Funktionen ohne Empfaenger (`Typ::neu(..)`) gibt es in dieser
// Runde nicht (docs/RUNDE45.md, „Bewusst weggelassen"). Wer sie schreibt,
// bekommt eine klare Ansage statt eines ratlosen Syntaxfehlers.
struct Punkt { x: i32, y: i32 }

impl Punkt {
    fn neu(x: i32, y: i32) -> Punkt { return Punkt{ x: x, y: y } }
}

fn main() -> i32 {
    return 0
}
