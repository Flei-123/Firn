// Module 'gapk' -- imported by tests/1657_const_from_module.fi.
// Its constants depend on each other in the "wrong" source order too.
export { OP_BASE, OP_LAST, WIDTH }

const OP_LAST: i32 = OP_BASE + 9
const OP_BASE: i32 = 30
const WIDTH: u64 = 8
