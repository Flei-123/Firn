// Mittlere Ebene: bindet die untere ein und hat DENSELBEN Funktionsnamen.
import tief
export { wert, doppelt }

fn wert() -> i32 {
    return tief.wert() * 10
}

fn doppelt(x: i32) -> i32 {
    return x + x
}
