// Module 'text' without an export list: everything is visible.
import modules.math

fn length(a: i32) -> i32 {
    // Access to another module from inside a module
    return math.square(a) + math.DREI
}
