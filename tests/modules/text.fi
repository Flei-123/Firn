// Modul 'text' ohne export-Liste: alles ist sichtbar.
import modules.math

fn length(a: i32) -> i32 {
    // Zugriff auf ein anderes Modul aus einem Modul heraus
    return math.square(a) + math.DREI
}
