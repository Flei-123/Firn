// expect_exit: 60
// Struct of 24 bytes: MEMORY class (hidden pointer to a copy).
struct R { a: i64, b: i64, c: i64 }

fn sum(r: R) -> i64 {
    return r.a + r.b + r.c
}

fn main() -> i32 {
    let r: R = R { a: 10, b: 20, c: 30 }
    return sum(r) as i32
}
