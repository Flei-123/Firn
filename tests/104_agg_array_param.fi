// expect_exit: 10
// Array als Wertparameter (Kopie beim Aufruf).
fn summe(a: [i32; 4]) -> i32 {
    var s: i32 = 0
    for i in 0 as usize..4 as usize {
        s = s + a[i]
    }
    return s
}

fn main() -> i32 {
    let a: [i32; 4] = [1, 2, 3, 4]
    return summe(a)
}
