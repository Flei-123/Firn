// SPDX-License-Identifier: MIT
// geo.dot -- the point. Public module of the package 'geo'.
export { Dot, dot_new, sum }

struct Dot { x: i32, y: i32 }

fn dot_new(x: i32, y: i32) -> Dot {
    return Dot { x: x, y: y }
}

fn sum(p: Dot) -> i32 {
    return p.x + p.y
}
