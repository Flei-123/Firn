// SPDX-License-Identifier: MIT
// help -- a module from the project's OWN sources (step 3 of the search
// order: 'source src').
export { sep }

fn sep() {
    var s: [u8; 1] = [32]
    syscall(1, 1, &s[0], 1)
}
