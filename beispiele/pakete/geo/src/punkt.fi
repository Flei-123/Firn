// geo.punkt — der Punkt. Oeffentliches Modul des Pakets 'geo'.
export { Punkt, punkt_neu, summe }

struct Punkt { x: i32, y: i32 }

fn punkt_neu(x: i32, y: i32) -> Punkt {
    return Punkt{ x: x, y: y }
}

fn summe(p: Punkt) -> i32 {
    return p.x + p.y
}
