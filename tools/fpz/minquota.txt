# tools/fpz/minquota.txt -- the floor for chapter Z (round B6).
#
# Not a target: a FLOOR. `fpz_checks` is the number of separate assertions
# `tools/fpz/fp_check.py` made and won. A change that removes one of them
# is a change that stops measuring something, and this line stops it.
#
# The two numbers that matter most are not here but IN the checks, because
# they only mean something together: 500 origins give 500 distinct
# canvases, AND the same path without the farbling gives exactly 1.
fpz_checks 16
