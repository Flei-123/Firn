// SPDX-License-Identifier: GPL-2.0-only
// tools/checked/rem_zero.fi -- remainder by zero (round 72).
fn feed(x: i64) -> i64 {
    return x
}

fn main() -> i32 {
    let a: i64 = feed(10)
    let b: i64 = feed(0)
    let c: i64 = a % b
    return c as i32
}
