// SPDX-License-Identifier: MPL-2.0
// tools/state/nostatic.fi -- the counter-check for section 3: a program
// without a single `static` must carry no data section of ours at all.
fn main() -> i32 {
    var a: [i32; 4] = [1, 2, 3, 4]
    return a[1]
}
