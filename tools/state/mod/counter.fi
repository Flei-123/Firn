// SPDX-License-Identifier: GPL-2.0-only
// tools/state/mod/counter.fi -- the global lives HERE, and only here.
export { hit, total, LIMIT }

static mut total: u64 = 0
static LIMIT: u64 = 7

fn hit() -> u64 {
    total = total + 1
    return total
}
