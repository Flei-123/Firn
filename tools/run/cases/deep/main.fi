// ROUND 84 -- the main file of the cache test. It never changes; the
// module next to it does. If the cache only looked at this file it would
// hand back the old binary, and that is exactly the mistake being ruled out.
import helper

fn main() -> i32 {
    return helper.value()
}
