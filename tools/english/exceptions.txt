# Bezeichner, die trotz Morphemtreffer richtig sind.
# lang = language (Kurzform in main.rs), kein deutsches "lang".
lang
LANG_NAME
LANG_NAME_LOWER
# flex-basis ist der Name der CSS-Eigenschaft (css-flexbox-1, 7.2.3);
# "basis" ist hier das englische Wort, nicht das deutsche.
P_FLEX_BASIS
S_FLEX_BASIS
basis
# Runde 64: DWARF-Konstante aus dem Standard (DWARF 4, Tabelle 7.17).
DW_LANG_C99
# Runde 76: "listen" ist hier das ENGLISCHE Verb aus listen(2) -- der
# Systemaufruf, mit dem ein Socket auf Verbindungen wartet. Das deutsche
# "Listen" (Plural von Liste) kommt in lib/std/net.fi nicht vor.
listen_tcp
listen_tcp_err
listener_port
listener_fd
listener_is_open
listener_from_fd
Listener
Listen
SYS_LISTEN
G_LISTEN
t_listen
# "element" ist hier das englische Wort: der Feldname `element` einer
# Registry-Eintragung im Minecraft-Protokoll (1.20.2+), nicht das deutsche
# "Element".
element_begin
# ROUND K3: "listen" is the ENGLISH verb again -- the same one as in
# listen(2) and in the TCP state LISTEN of RFC 793. The German "Listen"
# (plural of Liste) does not occur in lib/net/.
S_LISTEN
listen_step
net_listen
tcp_listen
