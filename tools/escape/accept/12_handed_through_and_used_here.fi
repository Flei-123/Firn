// expect_ok
// A pass-through function whose result is used in THIS frame. Only
// RETURNING it would be the error (rejected case 18).
fn hand(p: *mut i64) -> *mut i64 {
    return p
}

fn main() -> i32 {
    var x: i64 = 42
    let q: *mut i64 = hand(&x)
    return (*q) as i32
}
