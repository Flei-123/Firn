// expect_ok
// The way out, and it stays visible in the source. Case 01 of the rejected
// ones, with the attribute in front of it.
#[allow_escape]
fn sp_below() -> u64 {
    var a: u64 = 0
    return (&a) as u64
}

fn main() -> i32 {
    return (sp_below() & 1) as i32
}
