// expect_ok
// The address goes into a struct that is NOT returned and not handed
// anywhere. It dies with the frame, like the thing it points at.
struct Box { p: *mut i64 }

fn main() -> i32 {
    var x: i64 = 41
    var b: Box = Box { p: &x }
    *b.p = *b.p + 1
    return x as i32
}
