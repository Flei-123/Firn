// expect_ok
// Both the target and the source are locals of the SAME frame: nothing
// outlives anything.
fn give(dst: *mut u64, src: *mut i64) {
    *dst = src as u64
}

fn main() -> i32 {
    var x: i64 = 5
    var slot: u64 = 0
    give(&slot, &x)
    return (*(slot as *mut i64)) as i32
}
