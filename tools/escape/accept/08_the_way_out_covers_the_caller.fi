// expect_ok
// A vouched-for function must not send its CALLERS red instead: its summary
// stays empty.
const SLOT: u64 = 8192

#[allow_escape]
fn keep(p: *mut i64) {
    *(SLOT as *mut u64) = p as u64
}

fn main() -> i32 {
    var x: i64 = 5
    keep(&x)
    return 0
}
