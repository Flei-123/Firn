// expect_error: 5:5 the address of the local 'x' escapes through the return value
// An address smuggled through `as u64` is still an address.
fn bad() -> u64 {
    var x: i64 = 5
    return (&x) as u64
}

fn main() -> i32 {
    return (bad() & 1) as i32
}
