// expect_error: 5:5 the address of the local 'a' escapes through the return value
// `&a[0] + 8` still points into `a`.
fn bad() -> u64 {
    var a: [u8; 16] = [0 as u8; 16]
    return (&a[0]) as u64 + 8
}

fn main() -> i32 {
    return (bad() & 1) as i32
}
