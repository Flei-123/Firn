// expect_error: 5:5 the address of the local 'x' escapes through the return value
// The plain case out of docs/ROUND66.md gap 9.
fn bad() -> *mut i64 {
    var x: i64 = 5
    return &x
}

fn main() -> i32 {
    let p: *mut i64 = bad()
    return (*p) as i32
}
