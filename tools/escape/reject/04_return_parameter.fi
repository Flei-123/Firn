// expect_error: 4:5 the address of the local 'n' escapes through the return value
// A PARAMETER slot lies in the frame too -- `&p` is not `p`.
fn bad(n: i64) -> *mut i64 {
    return &n
}

fn main() -> i32 {
    let q: *mut i64 = bad(7)
    return (*q) as i32
}
