# tools/tlsb5/minquota.txt -- the floor for round B5.
#
# Not targets: FLOORS. Every number here was really measured on the run
# that added it, and the acceptance run fails when one of them falls.
# A number that goes UP is fine and belongs in the table of
# docs/ROUNDB5.md; a number that goes down is a regression and stops the
# build.
#
# `cert_refusals` has a line of its own on purpose. A verifier that
# accepts everything passes every positive case there is, so the count of
# cases that had to be REFUSED is the number that says whether the check
# is a check.
crypto_cases   647
cert_cases      26
cert_refusals   14
tls_cases       18
https_cases     14
jpeg_cases      16
img_cases       15
ui_cases        10
