# tools/layoutb2/minquota.txt -- the regression limits of round B2.
#
# The numbers this round reached. tools/layoutb2/run.sh stops when one of
# them falls, so a change to the layout that costs tests is noticed on the
# spot instead of in the next round.
#
#   wpt_b2   tests of corpus B2 passed against the official Web Platform
#            Tests (of 186; see docs/ROUNDB2.md for what the corpus is and
#            for the three groups that are counted separately)
#   reflow   documents of the whole harvest (471) whose layout at
#            800 -> 400 -> 800 is identical to the single layout at 800
#
wpt_b2 97
reflow 471
