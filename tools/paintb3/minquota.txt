# tools/paintb3/minquota.txt -- the regression limits of round B3.
#
# The numbers are the ones MEASURED when the round was finished, and they
# are limits and not targets: the acceptance run fails when the engine
# gets worse, never when it gets better. Whoever raises one of them has
# improved something and says in the commit what.
#
# wpt_ref  reference tests of tests/data/wpt-ref that pass AND are not
#          empty (tools/paintb3/reftest.py, of 541)
# glyphs   glyphs of the two fonts whose pixels agree with the second,
#          independent rasteriser (tools/paintb3/font_check.py,
#          129 of FirnSans + 264 of Ahem = 393)
wpt_ref 202
glyphs 393
