// SPDX-License-Identifier: MPL-2.0
// tools/checkidx/rem_zero.fi -- remainder by zero, at 64 bits.
fn feed(x: i64) -> i64 {
    return x
}

fn main() -> i32 {
    let a: i64 = feed(10)
    let b: i64 = feed(0)
    return (a % b) as i32
}
