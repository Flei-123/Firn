// SPDX-License-Identifier: GPL-2.0-only
// tools/checkidx/idx_u8.fi -- an octet array, the shape a parser uses.
fn feed(x: i32) -> i32 {
    return x
}

fn main() -> i32 {
    var buf: [u8; 16] = [0; 16]
    let i: i32 = feed(16)
    return buf[i as usize] as i32
}
