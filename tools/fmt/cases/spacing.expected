fn add(a: i32, b: i32) -> i32 {
    return a + b * 2 - 1
}
fn kinds(p: u64, d: *mut Diags) -> bool {
    var e: [u8; 4] = ".fi\0"
    let x: i32 = -1
    let y: i32 = -x
    let ok: bool = !(a == b) && c != d || e <= f
    let z: u64 = (&e[0]) as u64
    let w: u32 = vec_at[u32](&(*d).d_file, i)
    *(p as *mut u8) = 7 as u8
    let r: u64 = st + S_FREE + class * 8
    return ok
}
