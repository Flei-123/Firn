// a heading
//    an indented continuation


fn f()->i32{        // trailing, far away
    // inside
    let a:i32=1     // here too
        // deeper than the code
    return a
    }
struct S{
    a:u32,          // first
    bb:u32,         // second
}
