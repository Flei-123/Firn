import rt

import vec

fn main() -> i32 {
    var i: usize = 0

    while i < 10 {
        if i == 3 {
            continue
        } else {
            i = i + 1
        }
    }
    var t: Tree = Tree { a: 1, b: 2 }
    let f: fn(i32) -> i32 = fn(a: i32) -> i32 { return a + 1 }
    return 0
}
