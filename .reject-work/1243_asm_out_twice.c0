error: the register 'rax' is an output operand twice
  --> tests/neg/1243_asm_out_twice.fi:8:5
   |
 8 |     asm("mov rax, 1", out("rax") & a, out("rax") & b)
   |     ^^^^^^^^^^^^^^^^ here
   = note: every output needs a register of its own; the second one would overwrite the first
