error: operator '+' expects two operands of the same type, found f64 and i64
  --> tests/neg/f64_no_implicit_conversion.fi:6:18
   |
 6 |     let s: f64 = a + i
   |                  ^^^^^ here
   = note: the only implicit conversion is f32 -> f64; the other way round use 'as f32'
