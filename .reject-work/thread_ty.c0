error: '__thread_start' expects a pointer to the tid word as third argument, found u64
  --> tests/neg/thread_ty.fi:7:39
   |
 7 |     let r: i64 = __thread_start(1, 2, t)
   |                                       ^ here
   = note: the kernel writes the thread id there and zeroes it when the thread ends
