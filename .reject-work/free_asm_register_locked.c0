error: register 'rbx' is not allowed in the asm block (clobber)
  --> tests/neg/free_asm_register_locked.fi:3:5
   |
 3 |     asm("nop", clobber("rbx"))
   |     ^^^^^^^^^ here
   = note: allowed are only the caller-saved registers rax rcx rdx rsi rdi r8..r11 (including their narrow names); rbx, rbp, rsp and r12-r15 carry the frame or are callee-saved
error: the program has no function 'main'
  --> tests/neg/free_asm_register_locked.fi
  note: 'fn main() -> i32' is expected
2 errors found
