error: '++' is a statement, not an expression
  --> tests/neg/step_in_expression.fi:8:19
   |
 8 |     let y: i32 = x++
   |                   ^^ here
   = note: write it on a line of its own; there is no 'y = x++' here, because prefix and postfix inside an expression are a source of error
