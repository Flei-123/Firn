error: an asm block has at most one 'out' register WITHOUT a target
  --> tests/neg/1244_asm_two_bare_out.fi:5:64
   |
 5 |     let s: u64 = asm("mov rax, 1\nmov rdx, 2", out("rax"), out("rdx"))
   |                                                                ^^^^^ here
   = note: write 'out("rdx") p' — then the register goes into '*p' and there may be any number of them
