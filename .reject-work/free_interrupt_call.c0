error: 'ih' is an interrupt entry point and cannot be called
  --> tests/neg/free_interrupt_call.fi:9:5
   |
 9 |     ih()
   |     ^^ here
   = note: it ends with 'iretq' and expects the interrupt frame of the processor on the stack; only the IDT may point to it
