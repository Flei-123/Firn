// SPDX-License-Identifier: GPL-2.0-only
import modules.core.gccore
fn main() -> i32 {
    if !gc_init() { return 90 }
    let root: Gc[Ring] = gccore.ring_new(3, 4) catch gc_null[Ring]()
    let n: u64 = gc_live_objects()
    return n as i32
}
