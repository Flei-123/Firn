import modules.kern.gckern
fn main() -> i32 {
    if !gc_init() { return 90 }
    let wurzel: Gc[Ring] = gckern.ring_neu(3, 4) catch gc_null[Ring]()
    let n: u64 = gc_live_objects()
    return n as i32
}
